# Documentation for C++
1. Install the bun runtime on Windows
    ```bash
    powershell -c "irm bun.sh/install.ps1 | iex"
    ```
2. Clone this repository
3. Install dependencies 
    ```bash
    bun install
    ```
    
    ```bash
    bun run dev
    ```
4. Goto http://localhost:5173/docs/